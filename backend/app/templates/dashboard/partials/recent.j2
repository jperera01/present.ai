{% if recent|length == 0 %}

<p>You don't have any presentations...</p>

{% else %}

{% for line in recent %}
<li>
  <p>{{ line.name }}</p>
  <p>{{ line.length.minutes }} minutes, {{ line.length.seconds }} seconds</p>
</li>
{% endfor %}

{% endif %}


